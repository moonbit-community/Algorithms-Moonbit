# Persistent Trie

A persistent trie (prefix tree) that stores values associated with key sequences
and returns a new trie instance on every insertion.

This allows you to keep previous versions of the trie intact, which is useful
for time-travel queries or branching updates.
